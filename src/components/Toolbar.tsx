import {Toolbar as ToolbarMui} from "@mui/material"
import { styled } from "@mui/material/styles";

export const Toolbar = styled(ToolbarMui)(({ theme }) => ({
    justifyContent: 'space-between'
  }));

  